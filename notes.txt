->machine learning 
ml is all about traning the computer with data like human learn with the experience 
we give lots of data to the machine , machine predicts data on it's own and recoginses the pattrens 
-> uses of machine learning 
youtube recmendation watch history  
spam detection 
voice assistance 
face unlock on phones 
ML v/s AI v/s DL
AI-> it's makes the aplication smart 
ML -> it's a brain which learns from the data 
DL -> it's uses nureons , used for dealing the tough problems 
Data science is a feild where we learn the AI,ML,DL
-> types of ML 
_> supervised learing 
machine input and correct answers and system try to predict 
plays game of prediction 
99% works on this 
inome  credit score  loan
40,000    750         yes
20,000    600         NO
60,000    800         yes
30,000    580         NO
-> find for 
80,000    600        yes
_>unsupervised Learning 
we will find the pattrens 
it will group similar items , reduce complexity , spot outliners 
-> no prediction here 
-> ex -> google search 
only input no output 
_> Reinforcement learing
we don't teach them directly, it will learn on it's own where , good will be awarded and bad will be punished 
learning with trail and error 

_>EDA(Exploratory Data analysis )
1. problem definition 
2. data collection 
3. EDA 
4. Data preprocessing / cleaning 
5. Feature Selection & Engineering 
6. Split the DataSet 
7. Model Selection 
8. Model traning
9. Model Evaluation 
10.HyperParameter Tuning 
Step of EDA 
1. vewing the data 
 . head(),tail(),shape,info()
 . what columns do i have , what data  they are ?
2. Summary Statistics 
 . mean ,meadian , mode, std , min,max , quartiles 
 . helps to understand spread and central 
3.value counts
 . unique values in a coloumns 
 . great for categorical columns 
4. Missing vaue analysis 
5. visualization 
6. Target variable Exploration 
#importance of EDA 
-> identify the limitation 
-> guides for the data cleaning 
-> tells about the overview 
#data cleaning 
1. handling missing value 
. check with coloumns missing value
. stragies to handle 
-> drop it if it's few 
-> impute 
mean -> for numerical data 
mode -> categorical 
advanced -> linear regression , KNN or Interpolation 
2.Remove duplicates 
.drop the duplicate values 
3. fix data types 
4. handling inconsistent categories
5. detect and handle outliners
7. fix logic  or domanic error 
. invalid values to be replace 
#data prepocessing
1. encoding 
tanfroming the vaild data into usable format 
-> usable format , when the alogorithm is been converted to done in the format of numbers , if we have in the format of the string we will get an error so it's beter to convert into numbers for example in gender we have male and female , m-> 0 , F-> 1 this would help machine to understand clearly than the string values 
2 methods 
.label encoding gender 
.one hot encoding
region -> we craete a multiple column for example we have region , so we are creating north , south , east , west coloumn , if we take north than for the person living in north gets 1 remaining gets 0 , same for all the columns 
2. Feature tanfromation 
3. Feature Scaling  
-> bring the numerical values to a same scale 
normalization , scales between 0 and 1 
-> standardization 
#Feature Engineering 
ML model doesn't have domain logic , we are creating a new feature 
ex : date  ->  month   year    day 
    12/6/2005   6      2005      12
    16/8/2004   8       2004     16
#machine learning model to predict the charges 
#machine learning model to predict the heartDisease
#supervised alogorithm
regresserion algo 

diffrence between regression , classification alogorithm
-> if something is continutions it's regression 
-> we find patren 
#linear Regression 
create a plot -> scatter plot , a stright line is draw this is called as best fit line then the points are connect to predict 
->how to create the best fit line 
y = mx + b 
m is the slope , b is the intercept , x is the data point i need to prediction 
y is the predction 
slope -> if i am changing a data variable in x how much should i chance in y 
h(tetha(x)) = tetha(0) + tetha(1) tetha0 is intercept and tetha 1 is the slope 
-> residual error ->  predicted data points - actual data point is called as residual error
the best fit line is the line which has less redisdual error value 
# mean square error  -> cost function 
cost fuction : we will add all he residual error then we get the sum of the values , 
j (tetha0 , tetha1) = sumation (h tetha(x ) - y )^2 / 2m 
minimisze cost fumction by changing value of tetha0 and tetha1

gradeient descent 
{1,1}, {2,2}, {3,3} -> plot this 
use cost fuction and calculate the value , check for the residual error 
# which has the less cost function  this called as the gradient decent curve , find the the gobal minimum value 
repeat conversion theorem -> we use use this for createing the gradient decent function 
if my learning rate is low -> finding the global minimum is difficult so we keep the learing rate less 
 
 # multiple leanier regression 
 hyper plane 

#model evaluation tecniques 
 -> we need to predict the value 
  first we are comaring the x_tes to x_train and y_train to y_test -> using performance matrix 
  R^2 and adjusted R^2 
  R^2 = 1- (sum of residuals / sum of total) 
  r^2 value will always in positive value 
the result will be in decimals  0 - 1
adjusted R^2 -> age-> salary -> 81%
 if i add the new feature location location -> 88% 
 sleep -> 89% <-  model they aren't corretated -> so we use the adjusted R^2 
 formula => (1 - (1-R^2)(n- 1))/ n- p -1 
#overfitting , under fitting 
overfitting
traning -> getting good performing (low bais)
testing-> not good performing (high variance)
underfittng
traning -> no good performing (high bais)
testing-> not good performing (high variance)
                    M1 , M2 , M 3
traing accuracy     92  92    68
testing accuracy    64  90    64
                    o   good     u 

# to get a good model -> ridge and lesso regression 
we use the hyper parameter slope value -> 3D

#classification alogorithm
used to predict a category or class label for a given input 
we are doing binary   
